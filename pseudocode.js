class HanoiGame {
  // Set stacks in constructor

  // Until stacks are in order on a single stack that is not the starting stack,
  // prompt user for stack to move from and stack to move to

  // validate moves
  //  Check if moved disc is smaller than the topmost disc on the chosen tower
  //  Check if the move_from stack is not empty

  // Move disc
}
